query GetVendorByID($getVendorById: String!) {
    GetVendorByID(id: $getVendorById) {
        Errors {
            field
            message
        }
        Vendor {
            shortName
            longName
        }
    }
}
